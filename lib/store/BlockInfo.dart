class BlockInfo {
  BlockInfo(
      {this.currentValue,
      this.currentX,
      this.currentY,
      this.prevValue,
      this.prevX,
      this.prevY});

  int currentValue;
  int currentX;
  int currentY;

  int prevValue;
  int prevX;
  int prevY;
}
